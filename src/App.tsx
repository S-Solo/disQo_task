import React from "react";
import Layout from "components/Layout";
import Notepad from "containers/Notepad";

const App = () => {
  return (
    <Layout>
      <Notepad />
    </Layout>
  );
};

export default App;
